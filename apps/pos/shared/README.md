## `apps/pos/shared`

Cross-feature building blocks for POS:
- `shared/ui/*`: reusable POS-specific UI (no business logic)
- `shared/lib/*`: small helpers (formatting, keyboard utilities)
- `shared/types/*`: local types not owned by `@repo/types`

